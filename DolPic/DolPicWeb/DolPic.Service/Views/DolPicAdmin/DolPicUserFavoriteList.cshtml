@{
    ViewBag.Title = "회원 즐겨찾기 리스트";
    Layout = "~/Views/Shared/_Admin/_Layout.cshtml";

    var list = ViewBag.DataList;
}
@using PagedList.Mvc;
@using PagedList;

<link href="~/Css/PagedList.css" rel="stylesheet" />
<div>
    <div>
        <table border="1" cellpadding="0" cellspacing="0" class="notice_table">
            <tr class="notice_t_alert" style="width:30px">
                <th style="width:100px">아이디</th>
                <th style="width:150px">즐겨찾기 태그</th>
                <th style="width:150px">즐겨찾기 등록일</th>
            </tr>
            @foreach (var item in list)
            {
                <tr class="notice_t_alert" style="text-align:center; height:25px">
                    <td>@item.UserId</td>
                    <td>@item.HashTag</td>
                    <td>@item.RegDate</td>
                </tr>
            }
        </table>
    </div>
</div>
<div>
    @Html.PagedListPager((IPagedList)list, page => Url.Action("DolPicUserFavoriteList", new {page = page, UserId = ViewBag.UserId}))
</div>

<input type="button" value="회원 리스트" onclick="location.href = 'DolPicUserList';" />
